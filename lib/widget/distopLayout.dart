import 'package:flutter/material.dart';

class DasboardLayout extends StatelessWidget {
  const DasboardLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
